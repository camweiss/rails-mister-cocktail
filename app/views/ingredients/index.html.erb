<p id="notice"><%= notice %></p>

<h1>Ingredients</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ingredients.each do |ingredient| %>
      <tr>
        <td><%= ingredient.name %></td>
        <tr><%= link_to 'Show', ingredient %></tr>
        <tr><%= link_to 'Edit', edit_ingredient_path(ingredient) %></tr>
        <tr><%= link_to 'Destroy', ingredient, method: :delete, data: { confirm: 'Are you sure?' } %></tr>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ingredient', new_ingredient_path %>
